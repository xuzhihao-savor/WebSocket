package com.dianhun.netty;


import battleapp.netproto.Msg;
import battleapp.netproto.Struct;


/*public class InitializeDate  {


    public InitializeDate(int id ,long size){
        Msg.CS_AskMatchGameInfo.Builder msg = Msg.CS_AskMatchGameInfo.newBuilder();
        Struct.iPlayerInfo.Builder playerInfo = Struct.iPlayerInfo.newBuilder();
        Struct.iAvatarInfo.Builder avatar = Struct.iAvatarInfo.newBuilder();
        avatar.setCharid(103001);
        avatar.setPlayerid(member.getId());
        avatar.setElo(0);
        avatar.setName("");
        avatar.setRolelevel(1);
        avatar.setProgress(100);
        playerInfo.setAvatar(avatar);
        playerInfo.setJoinkey("");
        msg.setPlayerInfo(playerInfo);

        Struct.iLoginInfo.Builder loginInfo = Struct.iLoginInfo.newBuilder();
        loginInfo.setGameid(id);
        loginInfo.setMapid(113006);
        loginInfo.setTimeout(10000);
        loginInfo.setSid(size);
        loginInfo.setSid(0);
        loginInfo.setTime(System.currentTimeMillis());
        loginInfo.setRmode(0);
        loginInfo.setLink("");
        loginInfo.setSize(size);
        loginInfo.setIsGuide(false);
        loginInfo.setRobotelo(0);
        msg.setLoginInfo(loginInfo);

    }
}*/